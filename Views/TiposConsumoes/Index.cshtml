@model IEnumerable<prototipo2.Models.TiposConsumo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreditosNecesarios)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RequiereReserva)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HoraInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HoraFin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreditosNecesarios)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RequiereReserva)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HoraInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HoraFin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoConsumo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoConsumo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoConsumo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
