@model IEnumerable<prototipo2.Models.Turno>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Turno1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HoraInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HoraFin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoConsumoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Turno1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HoraInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HoraFin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoConsumoNavigation.IdTipoConsumo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTurno">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTurno">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTurno">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
